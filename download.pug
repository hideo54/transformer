html
    head
        meta(charset='utf-8')
        meta(name='viewport' content='width=device-width,initial-scale=1,maximum-scale=1')
        title transformer
        link(rel='stylesheet' type='text/css' href='style.css')
    body
        h1: a(href='./') transformer
        p ダウンロードが開始されます…
        p: a(href='' download='')#dl 開始されない場合はこちらをクリックしてください
        p: a(href='./')#top 戻る
        script.
            const s = location.search.substring(1);
            const params = s.split('&');
            let filename = '';
            for (let i = 0; i < params.length; i++) {
                const item = params[i].split('=');
                if (item[0] === 'filename') {
                    filename = item[1];
                }
            }
            const extension = filename.split('.').pop();
            document.getElementById('dl').href = './output.' + extension;
            document.getElementById('dl').setAttribute('download', filename);
            document.getElementById('dl').click();
            document.getElementById('top').click();

